console.log(
    "Haan to"
);
//just to check whether the file in static directory (which is public directory here )would be accessed each time even when we have a file in the local directory with the same name

//i have found that it does not matter whether we have a local file with same name or not the only thing which matters is the file in static directory